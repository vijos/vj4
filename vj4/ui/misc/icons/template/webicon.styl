@font-face
  font-family: $icon-font-name
  src: url('./{{ options.fontName }}.eot?{{ options.timestamp }}')
  src: url('./{{ options.fontName }}.eot?{{ options.timestamp }}#iefix') format('embedded-opentype'),
    url('./{{ options.fontName }}.woff2?{{ options.timestamp }}') format('woff2'),
    url('./{{ options.fontName }}.woff?{{ options.timestamp }}') format('woff'),
    url('./{{ options.fontName }}.ttf?{{ options.timestamp }}') format('truetype')
  font-weight: normal
  font-style: normal

.icon
  font-family: $icon-font-name !important
  speak: none
  font-style: normal
  font-weight: normal
  font-variant: normal
  text-transform: none
  line-height: 1
  -webkit-font-smoothing: antialiased
  -moz-osx-font-smoothing: grayscale

{% for icon in glyphs %}
.icon-{{ icon.name }}:before
  content: $icon-{{ icon.name }}
{% endfor %}
